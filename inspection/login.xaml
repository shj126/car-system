<Window x:Class="inspection.login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:inspection"
        mc:Ignorable="d"
        Title="登录" Height="500" Width="350.625" Icon="images/登录 用户.png" WindowStartupLocation="CenterScreen" WindowStyle="ThreeDBorderWindow" ResizeMode="NoResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Window.DataContext>
        <local:Identity />
    </Window.DataContext>
    <Grid Margin="0,0,0,-21">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="0,0,0,86" Grid.RowSpan="2">
            <Image Source="images/登录 用户.png" Width="150" Margin="25"></Image>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="0,10,0,120" Grid.RowSpan="2">
            <StackPanel Orientation="Horizontal" Margin="20 50 0 0">
                <TextBlock Text="用户名：" Height="20" Width="50" HorizontalAlignment="Center"  ></TextBlock>
                <TextBox Width="220" Height="25" VerticalContentAlignment="Center" x:Name="ed_user"></TextBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="20 10 0 0">
                <TextBlock Text="密   码：" Height="20" Width="50" ></TextBlock>
                <PasswordBox Width="220" Height="25" VerticalContentAlignment="Center" x:Name="ed_password"></PasswordBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox Height="20"  Margin="16 20 5 5" IsChecked="{Binding status}">学生登录</CheckBox>
                <CheckBox Height="20"  Margin="135 20 5 5" x:Name="ckbRemember">记住密码</CheckBox>
            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,36,0,28">
            <Button Style="{StaticResource ButtonPrimary}" hc:BorderElement.CornerRadius="15" Width="250" Height="40" Content="登录" x:Name="btn_login" Click="btn_login_Click" IsDefault="True"/>
            <Button Style="{StaticResource ButtonPrimary}" hc:BorderElement.CornerRadius="15" Width="250" Height="40" Content="注册" x:Name="btn_register" Margin="0 10 0 0" Click="btn_register_Click"/>
        </StackPanel>
    </Grid>
</Window>
